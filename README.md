# Real Estate Prototype

## Run
1. Ensure Node.js installed
2. cd backend && npm install
3. node server.js
4. Open frontend/index.html in a browser

The backend will create db/realestate.db using the schema on first run.